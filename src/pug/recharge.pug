extends layout/layout

block headTitle
  title 充值页面

block content
    .recharge-sec
      .sec-cont
        .card
          .trange-left-bottom
          .col
            .name ICCID号
            .no 89860<br/>2B012<br/>15915<br/>39738
          .col.col-r
            img(src="../global/image/icon_yidong.png", ).icon-card
          .tips-red 请仔细核对ICCID号，支付后将无法撤回
 
      .tabs-list#cardTypes
        .tab-one(data-type="18") 
          .price 18元
          .time 移动POS机（半年）
        .tab-one(data-type="35") 
          .price 35元
          .time 移动POS机（一年）
        .tab-one.active(data-type="100") 
          .price 100元
          .time 商户任性使用（一年）

      .sec-money   
        span 合计支付： 
        span.font-orange#moneyPrice ¥100.00             

      .btn-one
          a(href="javascript:void(0);").btn-bg-blue#payBtn 立即支付

block footer
  script.
    $(function(){
      $("#cardTypes").onClick('click','.tab-one',function(){
        const self = $(this);
        const selfPrice = self.attr('data-type');
        self.addClass('active').siblings().removeClass('active');
        $("#moneyPrice").html('¥'+selfPrice);
      });
      $("#payBtn").onClick('click',function() {
        const cardType = $("#cardTypes").find('.tab-one.active').attr('data-type');
        console.log(cardType);
        return;
        $.ajax({
            type: "POST",
            url: "",
            data: {iccid:account},
            success: function (result) {

            },
            error: function () {
              layer.sure('系统有误');
            }
          });
        });
    });
